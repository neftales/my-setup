- name: Clone my dotfiles repository
  become_user: "{{ user }}"
  ansible.builtin.git:
    key_file: $HOME/.ssh/id_ed25519
    repo: https://github.com/neftales/dotfiles.git
    dest: /home/neftales/dev/dotfiles
